E-Commerce theme — Open templates/index.html to preview.
To integrate into Django: copy HTML into templates/ and static folder to your staticfiles.
Ensure image paths use {% static %} if moving to Django templates.